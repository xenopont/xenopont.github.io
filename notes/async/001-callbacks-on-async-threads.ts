/**
 * Having multiple CPUs and remote execution processes that do not use
 * the CPU for some time, makes us thinking about running something else.
 * But then we need to get back to the main thread execution.
 * That's where the callbacks are helpful.
 */
